import axios from 'axios';
// import {refreshJWR} from './schemas';

export const queryVariants = {
  // refreshJWT: {
  //   queryKey: 'authRefresh',
  //   queryFunction: async () => {
  //     const response = await axios.get('auth/refresh/');
  //     return refreshJWR.parse(response.data);
  //   },
  // },
};
